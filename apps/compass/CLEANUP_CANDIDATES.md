# Compass Cleanup Candidates

- Generated at: 2026-07-02
- Scope: `E:\Github\NightRaven`
- Action taken: inventory only; no files deleted

## Safe To Remove After Closing Running Processes

These are local runtime/build artifacts. They are not source of truth and can be regenerated.

| Path | Reason | Check first |
|---|---|---|
| `.codex/compass-dev.out.log` | Local dev-server log | Stop Compass dev server if still running |
| `.codex/compass-dev.err.log` | Local dev-server error log | Stop Compass dev server if still running |
| `.codex/compass-dev.pid` | Local dev-server PID marker | Stop Compass dev server if still running |
| `.codex/autobuild-loop.pid` | Local autobuild PID marker | Confirm autobuild is stopped |
| `.codex/autobuild-stop.pid` | Local autobuild stop PID marker | Confirm autobuild is stopped |
| `.codex/autobuild-stop.log` | Local autobuild stop log | Confirm no debugging need remains |
| `.codex/autobuild-window.txt` | Local autobuild helper state | Confirm no active helper depends on it |
| `apps/compass/dist/` | Vite build output | Can be regenerated with `npm run build` |
| `apps/compass/node_modules/.vite/` | Vite dependency cache | Can be regenerated by Vite |

## Review Before Removing

These may be useful artifacts or local state. Remove only after deciding they are no longer needed.

| Path | Reason | Keep if |
|---|---|---|
| `.codex/project-state.md` | Local Codex/autobuild state, untracked | It contains useful run context not copied elsewhere |
| `docs/generated/compass/2026-07-03-say-commit-after-review-nightraven-prompt.md` | Generated Compass prompt artifact, untracked | The prompt should remain as project evidence |
| `.agents/` | Untracked agent assets/skills directory | It is intentionally part of local NightRaven agent setup |
| `.codex/` | Untracked local Codex state directory | It contains active local run state or logs needed for debugging |

## Do Not Remove As Cleanup

These are active source/product work from the current Compass implementation. They should be reviewed, staged, or intentionally reverted, not deleted as cleanup.

| Path | Reason |
|---|---|
| `apps/compass/PROJECT_STATE.md` | App evolution tracking source |
| `apps/compass/APP_FINAL_FORM_GOAL.md` | App final-form goal source |
| `apps/compass/MOCKUP_COMPONENT_TRACKER.md` | Mockup and unfinished component tracker |
| `apps/compass/APP_INTEGRITY_REPORT.md` | Integrity audit source |
| `apps/compass/VERSION_EVOLUTION_PLAN.md` | Next-version plan source |
| `apps/compass/CHANGELOG_EVOLUTION.md` | App evolution changelog |
| `apps/compass/server/evolutionTracker.ts` | Parses evolution files into snapshot data |
| `apps/compass/server/projectMonitor.ts` | File catalog and monitor engine |
| `apps/compass/src/components/evolution/` | Evolution UI surface |
| `apps/compass/src/components/files/` | File catalog UI surface |
| `apps/compass/src/components/detach/` | Detach readiness UI surface |
| `apps/compass/src/components/runs/` | Runs/evidence UI surface |
| `apps/compass/src/components/layout/CommandMenuBar.tsx` | Desktop menu surface |
| `apps/compass/src/utils/agentSettings.ts` | Agent token/profile settings model |

## Suggested Cleanup Order

1. Stop the Compass dev server and any autobuild helper.
2. Remove `.codex/*.log`, `.codex/*.pid`, and `.codex/autobuild-window.txt` if no debugging is needed.
3. Remove `apps/compass/dist/` before the next build if a clean build artifact is desired.
4. Keep or explicitly archive `docs/generated/compass/2026-07-03-say-commit-after-review-nightraven-prompt.md`.
5. Leave active Compass source files alone until they are staged, committed, or intentionally reverted.
